# name: Trigger auto deployment for webplayground-api

# # When this action will be executed
# on:
#   # Automatically trigger it when detected changes in repo
#   push:
#     branches: 
#       [ AzureLive ]
#     paths:
#     - '**'
#     - '.github/workflows/webplayground-api-AutoDeployTrigger-5245aa75-d484-46da-8e70-24d4662f262f.yml'

#   # Allow manual trigger 
#   workflow_dispatch:      

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     permissions: 
#       id-token: write #This is required for requesting the OIDC JWT Token
#       contents: read #Required when GH token is used to authenticate with private repo

#     steps:
#       - name: Checkout to the branch
#         uses: actions/checkout@v2

#       - name: Azure Login
#         uses: azure/login@v2
#         with:
#           client-id: ${{ secrets.WEBPLAYGROUNDAPI_AZURE_CLIENT_ID }}
#           tenant-id: ${{ secrets.WEBPLAYGROUNDAPI_AZURE_TENANT_ID }}
#           subscription-id: ${{ secrets.WEBPLAYGROUNDAPI_AZURE_SUBSCRIPTION_ID }}

#       - name: Build and push container image to registry
#         uses: azure/container-apps-deploy-action@v2
#         with:
#           appSourcePath: ${{ github.workspace }}/WebPlayground.Api
#           dockerfilePathKey: ./WebPlayground.Api/Dockerfile
#           registryUrl: ghcr.io
#           registryUsername: ${{ secrets.WEBPLAYGROUNDAPI_REGISTRY_USERNAME }}
#           registryPassword: ${{ secrets.WEBPLAYGROUNDAPI_REGISTRY_PASSWORD }}
#           containerAppName: webplayground-api
#           resourceGroup: WebPlayground_group
#           imageToBuild: ghcr.io/webplayground-api:${{ github.sha }}
#           _buildArgumentsKey_: |
#             _buildArgumentsValues_


