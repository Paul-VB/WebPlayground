{
  "version": "0.2.0",
  "configurations": [
	
    {
      "name": "Launch Firefox (VS Code only)",
      "type": "firefox",
      "request": "launch",
      "preLaunchTask": "Start npm dev server",
      "postDebugTask": "Kill npm dev server",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}"
    }
  ],
  "compounds": []
}